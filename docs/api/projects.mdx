---
title: "Projects"
description: "Choose credentials, understand 403 vs 404, and configure project defaults."
---

A project is the unit of keywords, providers, and rank history. This page covers
behavior that the generated API Reference does not: which credential can see or
create a project, how missing projects are hidden, and how defaults move
keywords.

## Credentials

<span id="list-projects"></span>

Project API keys are scoped to one project, so `listProjects` returns only that
project and `createProject` remains forbidden for them. A personal access token
returns every project the user belongs to and can create a new one when its
tier is at least `write`.

`project_id` must be the public `prj_...` id. If the project does not match a
project key, the API returns `403`. A PAT gets `404` when the user is not a
member, so project existence is not disclosed.

Only an owner's PAT can call `deleteProject`; project API keys receive `403`.

Self-hosters may cap the number of projects owned by one user with
`BISIBILITY_MAX_PROJECTS_PER_USER`. Current hosted limits live in
[Deployment options](/deployment-options).

## Project overview

`getProjectOverview` returns numeric health aggregates without paginating the
keyword list. `range` defaults to `28d` and accepts `7d`, `28d`, or `90d`.
`device` defaults to `all` and also accepts `desktop` or `mobile`. `tag` is an
optional exact tag name.

Positive `average_position_delta` means the average rank improved. Visibility
measures volume-weighted ranking strength in Google's Top 20. A value of 100
means every measured keyword ranks #1. Rankings below #20 do not add Visibility,
and shallow or failed checks are not treated as lost rankings.
`visibility_delta` is measured in percentage points. Rank aggregates and
distribution counts are `null` before the project has check data; a completed
check with no keyword in a bucket returns the real count `0`.

## Project defaults

`getProjectDefaults` returns the effective market and schedule. Projects without
a stored defaults row return schema-defined schedule defaults and a market
inferred from existing keywords, without creating a row. The `source` field is
`explicit`, `derived`, or `fallback`.

`updateProjectDefaults` stores scheduling intent. `country` and `device` in this
endpoint represent the project's current default keyword market, derived from
existing keywords. `location_key` is the primary location reference and can
point at a country, region, or city; send it with `device` to move eligible
keywords from the current default market to the new market.

The legacy `country` and `city` selectors still resolve to the same location,
but they are deprecated: send `location_key` instead. They are accepted until
bisibility 0.24.0, then removed. Omitting market fields updates only the
schedule and does not move keywords.

`country` and `device` must be supplied together unless `location_key` is
provided. `jitter_minutes` must be a whole number from `0` to `120`. Omitting
it uses `60`; `0` disables jitter.

Daily and weekly defaults give each keyword a stable interval phase, and
`timezone` does not move that phase. Monthly and custom cron schedules use
`timezone` as their wall-clock anchor.

## Related operations

- [`GET /projects`](/api-reference/projects/list-projects-visible-to-this-api-key) (`listProjects`)
- [`POST /projects`](/api-reference/projects/create-a-project) (`createProject`)
- [`GET /projects/{project_id}`](/api-reference/projects/get-one-project) (`getProject`)
- [`PATCH /projects/{project_id}`](/api-reference/projects/update-a-project) (`updateProject`)
- [`DELETE /projects/{project_id}`](/api-reference/projects/delete-a-project) (`deleteProject`)
- [`GET /projects/{project_id}/defaults`](/api-reference/projects/get-project-defaults) (`getProjectDefaults`)
- [`PATCH /projects/{project_id}/defaults`](/api-reference/projects/update-project-defaults) (`updateProjectDefaults`)
- [`GET /projects/{project_id}/overview`](/api-reference/analytics/get-a-numeric-project-overview) (`getProjectOverview`)

Exact request and response schemas live in the generated API Reference.
