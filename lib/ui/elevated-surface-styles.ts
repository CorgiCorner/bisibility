/** Hairline dividers on --bg-elev surfaces such as modals and cards. */
export const elevatedListClassName = "divide-y divide-border border-y border-border";

export const elevatedSectionDividerClassName = "border-t border-border";

/** Uppercase metric labels on elevated cards (KPI rows, budget sections). */
export const metricEyebrowClassName =
  "font-sans tabular-nums text-[10px] font-medium uppercase tracking-[0.08em] text-fg-muted";
